<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ctw_group3.foodsaver">
    <!-- Needed to connect to the internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FoodSaver">
        <activity android:name=".FoodOwner"></activity>
        <activity android:name=".LoginFoodOwner" />
        <activity android:name=".RegistrationFoodOwner" />
        <activity android:name=".Customer" />
        <activity android:name=".LoginCustomer" />
        <activity android:name=".RegistrationCustomer" />
        <activity android:name=".Submission" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>